{
  "buildCommand": "npm run fixture && npm run registry && npm run web:build && npx esbuild src/api-entry.ts --platform=node --bundle --format=esm --outfile=api/index.js --external:express",
  "functions": {
    "api/index.js": {
      "includeFiles": "{data,content,public}/**",
      "maxDuration": 60
    }
  },
  "crons": [
    {
      "path": "/api/cron/poll",
      "schedule": "0 6 * * *"
    }
  ],
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ]
}
